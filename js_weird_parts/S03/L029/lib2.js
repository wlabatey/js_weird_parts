window.libraryName = window.libraryName || "Lib 2";

/*

The above technique is commonly used in frameworks and libraries to check if a variable has already been set.

It uses the logical OR operator to check if the variable exists, before assigning it to another value.

This stops it from being unkindly overwritten by the current module.

*/