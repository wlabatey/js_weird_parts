var a;
console.log(a);

if (a === undefined) {
	console.log("a is undefined!");
} else {
	console.log("a is defined!");
}

/*

undefined is a special keyword that variables are set to in the creation phase, 

before their values are set later in the execution phase.

*/